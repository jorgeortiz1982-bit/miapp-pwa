name: macOS Workflow

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Mostrar versión de macOS
        run: sw_vers

      - name: Mostrar Xcode
        run: xcodebuild -version
